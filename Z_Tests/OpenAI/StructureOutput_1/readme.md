## Setup

```sh
python -m venv openaistructureoutput_venv #create the venv

openaistructureoutput_venv\Scripts\activate #activate venv (windows)
source openaistructureoutput_venv/bin/activate #(linux)
```

```sh
pip install -r requirements.txt #all at once
```